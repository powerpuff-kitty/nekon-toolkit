/** Existing V1 transcript and signature self-check; private-key ownership stays with the signer. */
export {
  createEnterpriseAuthorizationRedemptionWithSigner,
  type EnterpriseAuthorizationRedemptionProof,
  type EnterpriseAuthorizationRedemptionSignerInput,
} from "@nekon/client-runtime/application-enrollment-proof";
