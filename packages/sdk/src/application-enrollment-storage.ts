/** Same bound-storage adapter as the runtime; not a browser vault or automatic migration. */
export {
  openBoundApplicationEnrollmentVault,
  type ApplicationEnrollmentBinding,
  type ApplicationEnrollmentSecretVault,
} from "@nekon/client-runtime/application-enrollment-storage";
